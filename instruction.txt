# backend

cd backend
python -m uvicorn app.main:app --reload --port 8000

-------------------------------------------------------

# front end

cd frontend
python -m streamlit run app.py --server.port 8501